name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{secrets.HEROKU_API_KEY}}
          heroku_app_name: "search-project-be" #Must be unique in Heroku
<<<<<<< HEAD
          heroku_email: ${{secrets.HEROKU_EMAIL}}
          healthcheck: ${{secrets.HEROKU_URL}}
=======
          heroku_email: "difemove@gmail.com"
          healthcheck: "https://search-project-be.herokuapp.com/health"
>>>>>>> 360e6f1... chore: Node JS boilerplate: (#3)
